# Demo Workspace

Files here are intentionally minimal and (in the case of `buggy_math.py`)
intentionally broken so the agent has something real to do during the workshop.

Do not "pre-fix" before the demo.
